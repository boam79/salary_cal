{
  "name": "salary-cal-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/calc-core",
    "frontend"
  ],
  "scripts": {
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:e2e:app": "playwright test -c playwright.app.config.js",
    "build:app": "npm run build -w salary-cal-frontend-app"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "playwright": "^1.59.1",
    "supertest": "^7.1.1",
    "vitest": "^3.2.4"
  }
}
